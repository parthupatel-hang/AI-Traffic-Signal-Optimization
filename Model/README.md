# Decision Tree Model

`train_model.py` trains the project's Decision Tree classifier and writes:

- `model.pkl`
- `label_encoders.pkl`

The bundled source code is the reproducible part of the model pipeline. Binary model artifacts are intentionally not committed; run `python Model/train_model.py` to generate them locally.
